<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Enforce secure HTTPS connections with standard system + user trusted CAs.
         The target website is served over HTTPS, so cleartext traffic is
         disabled by default for maximum security while still allowing the
         system to validate SSL certificates correctly (SSL support). -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">vishal-flask-app.onrender.com</domain>
        <domain includeSubdomains="true">onrender.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
